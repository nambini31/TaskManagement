@model Domain.DTO.LeaveDto

<form method="post">
    <div class="form-group">
        <label for="Reason">Reason</label>
        <input type="text" class="form-control" id="Reasons" name="Reason" value="@Model.reason" required />
    </div>
    <input type="hidden" id="LeaveId" name="LeaveId" value="@Model.leaveId" />
    <button type="button" onclick="editSubmit()" class="btn btn-primary">Save</button>
</form>

